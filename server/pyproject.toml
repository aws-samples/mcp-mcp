[project]
name = "mcp-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=25.1.0",
    "mcp[cli]>=1.8.0",
]

[tool.uv.workspace]
members = [
    "tools",
]
